# Web-Server Deployment
# Manages the web-server pod lifecycle

apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-server
  namespace: mathwizz
  labels:
    app: web-server
    component: api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web-server
  template:
    metadata:
      labels:
        app: web-server
        component: api
    spec:
      containers:
        - name: web-server
          image: localhost/mathwizz/web-server:latest
          imagePullPolicy: Never  # Use local image from Kind
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: mathwizz-config
                  key: PORT
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: mathwizz-config
                  key: DB_HOST
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: mathwizz-config
                  key: DB_NAME
            - name: DB_USER
              valueFrom:
                configMapKeyRef:
                  name: mathwizz-config
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mathwizz-secrets
                  key: DB_PASSWORD
            - name: NATS_URL
              valueFrom:
                configMapKeyRef:
                  name: mathwizz-config
                  key: NATS_URL
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
